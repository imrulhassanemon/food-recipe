import React from 'react';

const RecipeDetails = async({params}) => {

    const reciepId = await params

    return (
        <div>
            
        </div>
    );
};

export default RecipeDetails;